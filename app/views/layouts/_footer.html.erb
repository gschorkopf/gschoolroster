<%= debug(params) if Rails.env.development? %>
  <div class="row" >
    <div class="navbar navbar-fixed-bottom" id="header">
      <div class="container">
        <div class="row">
          <div class="span3">
            <h5>
              <%= link_to "View Project on Github",
               "https://github.com/blairand/gschoolroster" %>
            </h5>
          </div>          

          <div class="span3">
            <h5>
              <%= link_to "Contributors", contributors_path %>
            </h5>
          </div>

          <div class="span3">
            <% if user_signed_in? %>
              <p class="lead">
                Signed in as <%= current_user.email %>,
                <%= link_to "sign out?", destroy_user_session_path, :method => :delete %>
              </p>
            <% else %>          
            <!--   <h5>
                <%= link_to "Sign Up", new_user_registration_path %>
                  or
                <%= link_to "Sign In", new_user_session_path %>
              </h5> -->
            <% end %>
          </div>
        </div>
      </div>
    </div> <!-- /.container -->

</div>
